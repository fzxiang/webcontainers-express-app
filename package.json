{
  "name": "webcontainers-express-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "npm run generate",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "node ./serve.js",
    "generate": "node ./scripts/generate.js /server"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@webcontainer/api": "^1.1.7",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
